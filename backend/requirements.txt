# Web
fastapi==0.111.0
uvicorn==0.30.1
pydantic==2.7.1

# Core ML (CPU-friendly, no native build needed)
numpy==1.26.4
pandas==2.2.2
scikit-learn==1.5.0
joblib==1.4.2

# Embeddings (uses HF + sentence-transformers)
sentence-transformers==2.6.1
transformers==4.41.2

# Torch CPU wheel (skip CUDA headaches)
# If you're on Windows/macOS/Linux CPU, keep this; if you have CUDA, see note below.
torch==2.3.0+cpu ; sys_platform == "darwin" or sys_platform == "win32" or sys_platform == "linux"

# For reading parquet artifacts
pyarrow==15.0.2

# Frontend
streamlit==1.36.0
requests==2.32.3
matplotlib==3.9.0
plotly==5.22.0

# Optional: load .env files for local development
python-dotenv==1.0.0
